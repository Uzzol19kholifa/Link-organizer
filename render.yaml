services:
  - type: web
    name: link-dedup-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false  # Render dashboard এ manually set করতে হবে
